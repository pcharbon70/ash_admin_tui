[
  # Dialyzer ignore file for known false positives
  # Add warnings to ignore here as they are encountered and verified as false positives
  #
  # Format:
  # {"lib/path/to/file.ex:line: pattern to match"}
  #
  # Example:
  # {"lib/ash_admin_tui/example.ex:42: Function will never be called"}
]
